/* ============================================================
   STARZ · 연결 설정
   ------------------------------------------------------------
   ▸ 아래 두 값을 채우면 "공유 모드"가 켜집니다.
     → 모든 팀원이 같은 데이터를 실시간으로 함께 봅니다.
   ▸ 비워 두면 "개인 모드"로 동작합니다.
     → 데이터가 이 브라우저에만 저장됩니다.

   값을 어디서 얻나요? (README.md 의 "팀원과 공유하기" 참고)
   Supabase 프로젝트 → Settings → API
     - SUPABASE_URL      : Project URL
     - SUPABASE_ANON_KEY : Project API keys 의 anon public 키

   ※ anon 키는 브라우저에 공개되도록 설계된 값이라 여기 넣어도 안전합니다.
   ============================================================ */
window.STARZ_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
};
