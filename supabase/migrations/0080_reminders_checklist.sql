-- ============================================================================
-- 0080 — 리마인드에도 하위 체크리스트(jsonb) 추가. CEO 투두·업무투두와 동일 구조.
-- ============================================================================

alter table public.reminders add column if not exists checklist jsonb not null default '[]'::jsonb;
